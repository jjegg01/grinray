[package]
name = "debug_tracer"
version = "0.2.0-alpha"
edition = "2021"

[dependencies]
grinray = { path = "../.." }
pyo3 = { version = "0.22", features = ["auto-initialize"]}
cgmath = "0.18.0"
rand = "0.8.5"
rand_xoshiro = "0.6.0"